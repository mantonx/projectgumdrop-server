type User {   
  id: ID!
  email: String,
  firstName: String
  lastName: String
  uuid: String
}

input userInput {
  email: String
  firstName: String
  lastName: String
}

type Query {
  getUser(email: String!): User
}

type Mutation {
  addUser(email: String!, firstName: String!, lastName: String!): User
}